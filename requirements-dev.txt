# Dev-only dependencies (CI, tests, linting). Production uses requirements.txt.

pytest>=8.3.0
pytest-cov>=5.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0          # FastAPI TestClient
ruff>=0.7.0
bandit>=1.7.0
mypy>=1.11.0           # optional; not yet wired into CI
